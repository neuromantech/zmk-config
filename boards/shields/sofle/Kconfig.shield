# Sofle dongle shield — SuperMini NRF52840 (nice_nano_v2)
# A keyless central for the Sofle split.
# Uses the shared keymap at config/sofle.keymap.

config SHIELD_SOFLE_DONGLE
    def_bool $(shields_list_contains,sofle_dongle)
